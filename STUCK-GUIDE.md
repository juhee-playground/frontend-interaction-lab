# Stuck Guide — 구현하다 막혔을 때

> 목적: 막혔을 때 바로 정답을 찾지 않고, **내가 어디서 막혔는지 파악하고 스스로 다음 단계로 갈 수 있게 하는 규칙**.

AI를 사용하지 않는 것이 목표가 아닙니다.

**생각하기 전에 AI에게 문제를 넘기는 습관을 줄이는 것**이 목표입니다.

---

## 막히면 먼저 이것부터

바로 검색하거나 AI에게 질문하지 않습니다.

먼저 아래 4가지를 적습니다.

```text
하려는 것:

현재까지 한 것:

내가 알고 있는 것:

정확히 막힌 것:
```

예:

```text
하려는 것:
multiple Accordion에서 클릭한 item을 닫고 싶다.

현재까지 한 것:
openValue를 string[]으로 만들었다.

내가 알고 있는 것:
클릭한 value가 배열에 있으면 제거해야 한다.

정확히 막힌 것:
배열에서 특정 값만 제거하는 JavaScript 문법이 기억나지 않는다.
```

`모르겠다`가 아니라 **무엇을 모르는지 한 문장으로 만들기**가 첫 번째 목표입니다.

---

# 막힘 탈출 순서

## STEP 1 — 5분 혼자 생각하기

최소 5분은 직접 고민합니다.

이때 코드를 계속 두드리기보다 아래 질문에 답해봅니다.

- 지금 만들려는 결과는 무엇인가?
- 현재 상태는 무엇인가?
- 이벤트 전 상태는?
- 이벤트 후 상태는 어떻게 되어야 하는가?
- 필요한 값은 이미 가지고 있는가?
- 문제는 React인가, JavaScript인가, TypeScript인가?

코드로 바로 표현이 안 되면 한글로 먼저 씁니다.

```text
클릭한 값이 현재 배열에 없다
→ 배열에 추가한다.

클릭한 값이 현재 배열에 있다
→ 배열에서 제거한다.
```

한글로 설명할 수 있다면 **설계보다 문법에서 막힌 것일 가능성이 높습니다.**

---

## STEP 2 — 막힘 종류 분류하기

### A. 문법이 기억 안 난다

예:

- 배열에 값이 있는지 확인하는 메서드가 기억 안 남
- 배열에서 특정 값을 제거하는 방법이 기억 안 남
- `useState` 타입 문법이 기억 안 남
- `createContext` 문법이 기억 안 남

이 경우 **공식 문서를 찾아봐도 됩니다.**

추천 순서:

1. 내가 기억나는 형태로 먼저 작성
2. TypeScript / 브라우저 에러 읽기
3. MDN / React 공식 문서 확인
4. 문서를 닫고 다시 작성

목표는 API 이름을 전부 암기하는 것이 아닙니다.

---

### B. 로직이 안 떠오른다

예:

- multiple toggle을 어떻게 만들어야 할지 모르겠다.
- 같은 item을 다시 눌렀을 때 어떤 상태로 만들어야 할지 헷갈린다.

이 경우 코드를 찾기 전에 **입력 → 다음 상태**를 적습니다.

```text
현재: ["item-1"]
클릭: "item-2"

다음 상태: ?
```

또는:

```text
현재: ["item-1", "item-2"]
클릭: "item-1"

다음 상태: ?
```

다음 상태를 말할 수 있다면 그다음에 구현 방법을 생각합니다.

---

### C. React 구조/설계가 안 잡힌다

예:

- 이 상태를 Accordion이 가져야 하나 Item이 가져야 하나?
- Context를 왜 쓰는지 모르겠다.
- 이 값을 Trigger까지 어떻게 전달해야 할지 모르겠다.

이 경우 구현을 잠시 멈추고 아래를 적습니다.

```text
상태:
누가 변경:
누가 읽음:
몇 단계 아래까지 필요:
```

예:

```text
상태:
현재 열린 item value

누가 변경:
AccordionTrigger

누가 읽음:
AccordionContent

몇 단계 아래까지 필요:
Accordion → Item → Trigger / Content
```

그다음 질문합니다.

> 이 상태를 가장 위에서 공유해야 하는가?
> props로 전달해도 되는가?
> Context가 실제로 필요한가?

---

### D. TypeScript 에러에서 막힌다

먼저 에러 메시지를 끝까지 읽습니다.

그리고 다음 세 가지를 확인합니다.

```text
내가 넣은 값의 타입:
컴포넌트/함수가 기대하는 타입:
둘의 차이:
```

타입을 무조건 `any`로 바꾸거나 assertion부터 넣지 않습니다.

에러가 이해되지 않을 때는 AI에게 **에러의 의미만** 물어봐도 됩니다.

---

# 10~15분째 해결되지 않는다면

이제 AI를 사용해도 됩니다.

단, 처음부터 완성 코드를 요청하지 않습니다.

## Level 1 — 방향만 확인

```text
Accordion Day 4 하는 중이야.

하려는 것:
multiple toggle

내 생각:
클릭한 value가 배열에 없으면 추가,
있으면 제거해야 할 것 같아.

여기까지 방향이 맞는지만 알려줘.
코드는 주지 마.
```

---

## Level 2 — 힌트 요청

```text
방향은 이해했는데 구현 방법이 안 떠올라.

완성 코드는 주지 말고
어떤 배열 메서드를 생각해보면 되는지만 힌트 줘.
```

---

## Level 3 — 내 코드 리뷰

내가 먼저 작성한 코드를 보여줍니다.

```text
내가 여기까지 구현했어.

어디가 잘못됐는지 알려줘.
수정된 전체 코드는 아직 주지 마.
```

---

## Level 4 — 일부 코드 확인

여전히 해결되지 않으면 **막힌 부분만** 코드를 확인합니다.

전체 Accordion 코드를 받지 않습니다.

---

## Level 5 — 정답 확인

15~20분 이상 같은 문제에서 멈춰 있다면 정답을 확인해도 됩니다.

하지만 정답 확인으로 공부를 끝내지 않습니다.

```text
정답 확인
↓
왜 이렇게 되는지 설명
↓
정답 닫기
↓
해당 코드 삭제
↓
내 손으로 다시 작성
```

다시 작성할 수 있어야 완료입니다.

---

# 절대 하지 않을 것

- 에러가 뜨자마자 전체 코드를 AI에 붙여서 "고쳐줘" 하기
- 구현 시작 전에 정답 코드부터 보기
- 이해하지 못한 코드를 복사해서 통과시키기
- TypeScript가 귀찮다고 바로 `any` 사용하기
- 30분 내내 한 문제만 붙잡고 자책하기
- 막힌 사실을 실패로 기록하기

---

# 막힘 기록

막힌 것은 따로 기록합니다.

```md
## YYYY-MM-DD

### 하려던 것

multiple Accordion toggle

### 막힌 것

배열에서 클릭한 value를 제거하는 방법

### 막힌 종류

- [x] JavaScript 문법
- [ ] React 개념
- [ ] 상태 설계
- [ ] TypeScript
- [ ] 기타

### 내가 처음 생각한 방법

...

### 확인한 것

...

### 지금은 설명할 수 있는가?

...
```

같은 항목에서 여러 번 막히면 그게 **다음 복습 대상**입니다.

---

# 30분 공부 중 권장 흐름

```text
0~5분
기억에서 꺼내기 / 오늘 상태 설계

5~20분
직접 구현

막힘 발생
↓
5분 직접 고민
↓
막힘 종류 분류
↓
문법 → 공식 문서
로직/설계 → 한글로 상태 변화 작성
그래도 안 됨 → AI 힌트

20~27분
구현 마무리 또는 막힌 부분 이해

27~30분
코드 닫고 설명 + Learning.md 기록
```

30분 안에 오늘 미션을 다 못 끝내도 괜찮습니다.

**이해하지 못한 채 완성하는 것보다, 어디에서 막혔는지 정확히 알고 끝내는 것이 더 낫습니다.**

---

# AI에게 질문할 때 템플릿

```text
[Accordion Day N — 막힘]

하려는 것:

현재 코드/상태:

내가 생각한 해결 방향:

정확히 막힌 부분:

원하는 도움:
정답 코드 말고 힌트만 줘.
```

필요하면 도움 수준을 명시합니다.

```text
Level 1: 방향만 확인
Level 2: 개념/메서드 힌트
Level 3: 내 코드 문제점만 찾기
Level 4: 막힌 부분 코드
Level 5: 정답 확인
```

---

# 기억할 한 문장

> 막히지 않는 것이 목표가 아니라, **막혔을 때 스스로 문제를 작게 만들 수 있는 개발자**가 되는 것이 목표다.
