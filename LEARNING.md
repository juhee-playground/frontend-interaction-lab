# Accordion Learning Guide

## 이번 주 목표

이번 주의 목표는 **Accordion 기능을 많이 완성하는 것**이 아닙니다.

> 코드를 한동안 보지 않아도 Accordion의 상태 흐름을 설명하고, 기본
> 구조를 다시 구현할 수 있는 상태를 만든다.

완성 속도보다 **기억에서 꺼내기 → 직접 구현하기 → 막힌 이유 확인하기**를
반복합니다.

---

## 매일 공부 방법 --- 30분

### 1. 기억에서 꺼내기 --- 5분

전날 코드를 열기 전에 먼저 기억나는 내용을 적습니다.

- 어제 무엇을 구현했는가?
- 필요한 상태는 무엇이었는가?
- 그 상태는 누가 가지고 있었는가?
- 사용자가 어떤 행동을 하면 상태가 변했는가?
- 어떤 컴포넌트가 그 상태를 읽었는가?

기억이 안 나도 바로 코드를 보지 않습니다.\
틀려도 괜찮으니 먼저 적어봅니다.

---

### 2. 직접 구현하기 --- 15\~20분

AI 없이 먼저 구현합니다.

막히는 문법이나 로직이 있으면 바로 검색하거나 정답을 보지 않고 주석으로
남깁니다.

예:

```ts
// TODO: 배열에 이 value가 있는지 어떻게 확인했지?
// TODO: 현재 value만 배열에서 제거하려면?
```

막힌 부분 때문에 전체 구현을 멈추지 말고, 아는 부분부터 진행합니다.

---

### 3. 막힌 부분 확인하기 --- 5분

직접 고민한 뒤에도 해결되지 않는 부분만 확인합니다.

AI를 사용할 때는 가능한 한 다음 순서로 질문합니다.

1.  개념 확인
2.  힌트 요청
3.  내가 작성한 코드 리뷰
4.  그래도 해결되지 않을 때 정답 코드 확인

좋은 질문:

> multiple에서 클릭한 value가 이미 열려 있는지 판단해야 하는데 어떤
> 방향으로 생각하면 될까? 코드는 주지 마.

피할 질문:

> multiple Accordion 완성 코드 만들어줘.

---

### 4. 코드 없이 설명하기 --- 2\~3분

마지막에는 코드를 닫고 오늘 구현한 흐름을 말로 설명합니다.

예:

> Accordion이 열린 상태를 가지고 있다.\
> Trigger를 클릭하면 자신의 item value를 이용해 toggle을 호출한다.\
> 상태가 변경되면 Content가 자신의 item이 열렸는지 확인하고 표시한다.

설명할 수 없다면 아직 외운 것이 아니라 이해가 덜 된 부분입니다.

---

## 매일 기록

공부가 끝나면 아래 형식으로 짧게 기록합니다.

### YYYY-MM-DD --- Day N

#### 오늘 한 것

-

#### 기억에서 바로 나온 것

-

#### 막힌 것

-

#### 왜 막혔는가?

- 문법을 잊음
- React 개념이 헷갈림
- 상태 설계가 안 됨
- TypeScript 타입이 안 잡힘
- 기타:

#### 확인 후 이해한 것

-

#### 코드 없이 설명

#### 내일 다시 확인할 것

-

---

## AI 사용 규칙

- 시작 후 최소 15분은 직접 고민한다.
- 처음부터 완성 코드를 요청하지 않는다.
- 먼저 내 생각이나 코드를 만든다.
- 가능하면 힌트만 요청한다.
- AI 답변을 이해하지 못한 채 복사하지 않는다.
- 정답을 봤다면 그대로 끝내지 않고 다시 직접 작성한다.

---

## 이번 주 성공 기준

이번 주가 끝났을 때 아래 질문에 코드 없이 답할 수 있으면 성공입니다.

- Accordion의 열린 상태는 어디에 있는가?
- Trigger를 클릭하면 어떤 흐름으로 Content가 열리는가?
- `AccordionContext`는 왜 필요한가?
- `AccordionItemContext`는 왜 필요한가?
- `single`과 `multiple`은 상태 구조가 왜 다른가?
- `disabled`를 `<button disabled>`로 처리하면 어떤 이점이 있는가?
- Enter / Space를 직접 구현하지 않아도 되는 이유는 무엇인가?
- ArrowUp / ArrowDown은 왜 별도 처리가 필요한가?
- Compound Component 패턴을 사용한 이유는 무엇인가?

그리고 가장 중요한 마지막 기준:

> 기존 구현을 그대로 보지 않고 기본 Accordion 구조를 다시 만들 수
> 있는가?
